using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Collections.Generic;
using Monsters.monster;
using Monsters.core;
using Monsters.mechanics;
namespace Monsters.Tests
{
    [TestClass]
    public class MonsterTests
    {
        [TestMethod]
        public void Test_Monster_Create()
        {
             //Monster m = new Monster("test",100,new List<Move>(),new monster.StatsManager(0,0,0,0,0,0));


            //Assert.AreEqual("test", m.sName);
        }

    }
}
